얕은 비교를 통해 두 값이 같은지 비교하는 함수

```md
1. 참조가 동일한 경우 true 반환 (`===` 연산자를 활용하여 비교 )
2. 타입이 다른 경우 false 반환
3. 객체 타입이며 null이 아닌 경우
   3-1. 객체의 키 개수가 다르면 false 반환
   3-2. 모든 키를 순회하며 값을 비교
      3-2-1. 값이 다르면 false 반환
   3-3. 순회가 끝나면 true 반환
4. 객체가 아닌 경우, `===` 연산자를 활용하여 비교
```
